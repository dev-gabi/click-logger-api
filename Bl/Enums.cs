namespace Bl
{
    internal class Enums
    {
        public enum Buttons
        {
            Login,
            Logout
        }

        public enum AuthContextItems
        {
            UserId,
            LoginUserStatsId,
            Email
        }
    }
}
